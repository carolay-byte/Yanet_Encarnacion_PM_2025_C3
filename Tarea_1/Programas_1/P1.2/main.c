#include <stdio.h>
#include <stdlib.h>

/* El segundo programa, invierte datos */

int main(void)
{
    int A, B, C, D;
    printf("Ingrese cuatro variables de tipo entero:\n");
    scanf("%d %d %d %d", &A, &B, &C, &D);
    printf ("\n %d %d %d %d ", D, C, B, A);
    return 0;
}
